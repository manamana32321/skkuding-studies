{
  "type": "module",
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "bcrypt": "^5.1.1",
    "dotenv-cli": "^7.4.2",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^5.20.0"
  },
  "scripts": {
    "start": "prisma generate && node app.js",
    "build": "dotenv -e ../.env.development npx prisma migrate dev --name init"
  }
}
